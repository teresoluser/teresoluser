package com.teacher.rest.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeTeacherResourceIT extends TeacherResourceTest {

    // Execute the same tests but in native mode.
}